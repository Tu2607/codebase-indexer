# smoke_repo fixture

Deterministic mini-repository used by the smoke tests in
`tests/test_smoke.py`. Do not add unrelated files or churn existing
symbols; the smoke tests assert on known relative paths and search
terms defined here.
